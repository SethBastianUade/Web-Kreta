# Dependencias (el CSS compilado SÍ se commitea → deploy buildless)
node_modules

# Ignorar agentes de claude
README.md
skills-lock.json
.agents

# Worktrees temporales de agentes (desechables, no versionar)
.claude/worktrees/

# Config de tooling local
.opencode.jsonc

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
