# Dependencias (el CSS compilado SÍ se commitea → deploy buildless)
node_modules

# Ignorar agentes de claude
README.md
skills-lock.json
.agents

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
