{
  "name": "web-kreta",
  "private": true,
  "version": "1.0.0",
  "description": "Landing de Kreta (tecnología + finanzas). Tailwind compilado, estilo Minimal Editorial.",
  "scripts": {
    "build": "tailwindcss -i assets/css/tailwind-src.css -o assets/css/tailwind.css --minify",
    "watch": "tailwindcss -i assets/css/tailwind-src.css -o assets/css/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^3.4.17"
  }
}
